export interface OverlaySettings {
  opacity: number;
  scale: number;
  positionX: number;
  positionY: number;
  rotation: number;
  tiltX: number;
  tiltY: number;
}

export interface CameraDevice {
  deviceId: string;
  label: string;
}
